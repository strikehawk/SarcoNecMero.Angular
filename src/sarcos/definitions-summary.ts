module snm.sarcos {
    export interface IllustrationPanneauSummary {
        referenceSarco: string;
        panneau: string;
        illustrationId: number;
        chemin: string;
        label?: string;
    }
}